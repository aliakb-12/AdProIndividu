app = "keya12"        # replace with your Fly.io app name
primary_region = "sin" # choose a region close to you

[build]
dockerfile = "Dockerfile"

[[services]]
internal_port = 8080
protocol = "tcp"

[[services.ports]]
port = 80
handlers = ["http"]